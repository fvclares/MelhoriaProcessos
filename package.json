{
  "name": "inteligencia-operacional",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "lint": "node --check js/auth.js && node --check js/api.js && node --check js/app.js && node --check js/admin.js && node --check js/analytics.js && node --check js/evolution.js && node --check js/semantic.js && node scripts/check-edge-functions.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run lint && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
